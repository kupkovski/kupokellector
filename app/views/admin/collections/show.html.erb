<p style="color: green"><%= notice %></p>

<%= render 'admin/collections/record', collection: @collection %>

<div>
  <%= link_to "Edit this collection", edit_admin_collection_path(@collection) %> |
  <%= link_to "Back to collections", admin_collections_path %> |
  <%= link_to "Manage Cards", admin_collection_cards_path(@collection) %>

  <p></p>
  <%= button_to "Destroy this collection", admin_collection_path(@collection), method: :delete, data: { turbo_confirm: "Are you sure?" }  %>
</div>
