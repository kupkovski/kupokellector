<p style="color: green"><%= notice %></p>

<%= render @card %>

<div>
  <%= link_to "Edit this card", edit_admin_card_path(@card) %> |
  <%= link_to "Back to cards", admin_cards_path %>

  <%= button_to "Destroy this card", admin_card_path(@card), method: :delete, data: { turbo_confirm: 'Are you sure?' } %>
</div>
